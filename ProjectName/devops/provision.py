import sys

if __name__ == '__main__':
    # if local:vagrant
    # Create vagrant machines and provision with ansible
    # if local:docker
    # create docker images with folder mounting to this project directory and run them
    # if local:vagrantdocker
    # Create vagrant machine with Ubuntu trusty server and docker installed, create docker images and run them on this
    # vagrant machine
    # if env=any
    # Create images with packer, upload image to a hub, create resources with this images with terraform

    # if ansible $(tag):
    # Run ansible task with tag passed as argument
    pass
